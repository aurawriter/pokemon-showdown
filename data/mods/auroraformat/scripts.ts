export const Scripts: ModdedBattleScriptsData = {
	inherit: 'gen9',
	gen: 9,
	/*init() {
    for (const id in this.data.Learnsets) {
      if (!this.data.Learnsets[id].learnset) this.data.Learnsets[id].learnset = {};
      this.data.Learnsets[id].learnset.essenceburst = ['9L1'];
    }
  },*/
};
